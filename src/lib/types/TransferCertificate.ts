export interface TransferCertificate {
  studentName: string;
  fatherName: string;
  registrationNumber: string;
  admissionNumber: string;
  tcNumber: string;
  session: string;
  class: string;
  issueDate: string;
  pdfUrl: string;
}